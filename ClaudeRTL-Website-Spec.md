# Claude RTL — موقع الأداة (سبيك بناء Cursor)

> الهدف: موقع عربي (RTL) بالفصحى على **claude-rtl.grwlab.net**، **بنفس هوية التطبيق بالضبط**.
> الأولوية القصوى: **هوية بصرية واحدة** — نفس الألوان/الخطوط/الإحساس بتاع `bubble.html`.
> ثابت (Static HTML/CSS/vanilla JS) عشان يترفع مباشرةً على docroot الصب دومين بدون build.

---

## 0) معايير الكود (التزم بيها)
- وحدات **rem**، شبكة **8px**، base 10px (أي 16px = 1.6rem).
- **Design tokens** كـ CSS custom properties (المصدر الوحيد للهوية — تحت).
- **ممنوع framer-motion** أو أي مكتبة أنيميشن: استخدم **CSS transitions + IntersectionObserver** للكشف عند التمرير.
- HTML دلالي، `dir="rtl"` و`lang="ar"`، وصول كامل (alt، focus states، `prefers-reduced-motion`).
- متجاوب (موبايل/تابلت/ديسكتوب)، خفيف وسريع، بدون مكتبات تقيلة.

---

## 1) الهوية البصرية — المصدر الوحيد (`assets/tokens.css`)
> **نفس قيم التطبيق.** لازم الموقع والتطبيق يطابقوا بعض.
```css
:root{
  /* ألوان Claude */
  --coral:#C15F3C;        /* الأكسنت الأساسي */
  --coral-bright:#D97757; /* أكسنت على الداكن */
  --tan:#D4A27F;
  --cream:#F4F3EE;        /* Pampas */
  --paper:#FAF9F5;
  --ink:#191919;
  --muted:#6B675F;
  --line:#E7E3D8;

  /* المسافات (8px grid) */
  --s-1:0.8rem; --s-2:1.6rem; --s-3:2.4rem; --s-4:3.2rem; --s-6:4.8rem; --s-8:6.4rem;
  --radius:1.4rem;
  --shadow:0 1.2rem 3.2rem rgba(25,25,25,.10);

  /* الخطوط */
  --font-ar:"IBM Plex Sans Arabic","Tajawal",-apple-system,"SF Arabic",sans-serif;
  --font-serif:"Source Serif 4","Newsreader",ui-serif,Georgia,serif; /* للعناوين الكبيرة (لمسة Claude) */

  /* أسطح الوضع الفاتح */
  --bg:var(--paper); --card:#fff; --surface:var(--cream); --text:var(--ink); --text-soft:var(--muted); --border:var(--line); --accent:var(--coral);
}
@media (prefers-color-scheme: dark){
  :root{
    --bg:#262220; --card:#2E2A27; --surface:#211E1C; --text:var(--cream);
    --text-soft:#B1ADA1; --border:#3A3531; --accent:var(--coral-bright);
  }
}
```
- اللوجو = **نفس أيقونة التطبيق** (شيفرة الـ chevron RTL) — تتكرر كـ favicon وفي الـ nav وOG image.
- خط عربي ويب: **IBM Plex Sans Arabic** (self-host أو Google Fonts). العناوين الكبيرة ممكن serif خفيفة للمسة Claude.

---

## 2) هيكل الموقع
```
/ (docroot الصب دومين)
├── index.html        ← صفحة الهبوط (RTL، عربي فصحى)
├── privacy.html      ← سياسة الخصوصية
├── 404.html
├── assets/
│   ├── tokens.css    ← الهوية (فوق)
│   ├── styles.css    ← التنسيقات
│   ├── app.js        ← IntersectionObserver reveals + كشف macOS + سنة الفوتر
│   └── img/          ← logo.svg, icon.png, og-image.png, screenshots, demo.gif
└── download/
    └── ClaudeRTL-1.0.0.dmg   ← الملف الموثّق
```

---

## 3) صفحة الهبوط `index.html` — الأقسام والمحتوى (فصحى)

**Meta/SEO/OG (مهم للمشاركة على السوشيال):**
- `<title>Claude RTL — عرضٌ صحيح للعربية في Claude</title>`
- description: «أداة macOS مجانية تُصلِح اتجاه النص العربي في تطبيق Claude فورًا، مع قراءة صوتية وتمييز للكلمات.»
- og:title / og:description / og:image (`assets/img/og-image.png` 1200×630) / twitter:card=summary_large_image.

**1. شريط علوي (nav):** اللوجو + اسم "Claude RTL" + زر **«نزّل لـ macOS»**.

**2. Hero:**
- عنوان: **«اقرأ العربية في Claude كما يجب أن تكون.»**
- وصف: «أداة مجانية لنظام macOS تُصلِح اتجاه النص العربي في تطبيق Claude فورًا — حدِّد النص، فتظهر فقاعةٌ تعرضه بشكلٍ صحيح، مع قراءةٍ صوتية وتمييزٍ للكلمات أثناء القراءة.»
- زر أساسي: **«نزّل لـ macOS»** + سطر صغير: «مجاني · يتطلب macOS 13 أو أحدث».
- صورة/لقطة للبالون بجانب نص Claude (before/after لو أمكن).

**3. المشكلة → الحل:**
«النص العربي في تطبيق Claude لسطح المكتب يظهر مبعثرًا ومختلط الاتجاه. Claude RTL يحلّ ذلك دون انتظار أي تحديث — يعمل فوق التطبيق مباشرةً.»

**4. المميزات (شبكة 6 بطاقات):**
1. **اتجاهٌ صحيح للنص** — RTL سليم، مع إبقاء الشيفرة البرمجية من اليسار إلى اليمين.
2. **تنسيق Markdown** — عناوين وقوائم وروابط تظهر منسّقة.
3. **قراءة صوتية بتمييز الكلمات** — استمع للنص، وكل كلمة تُضاء أثناء قراءتها.
4. **خصوصية كاملة** — كل شيء يعمل على جهازك، ولا تُرسَل أي بيانات.
5. **سريع وخفيف** — يعيش في شريط القوائم، ويظهر عند الحاجة فقط.
6. **فاتح وداكن** — يتناغم تلقائيًا مع مظهر نظامك.

**5. كيف يعمل (٣ خطوات):**
1. نزّل ملف ‎.dmg‎ وافتحه.
2. اسحب **Claude RTL** إلى مجلد Applications.
3. عند أول تشغيل، فعِّل إذن **Accessibility** — ثم حدِّد نصًا عربيًا في Claude.

**6. عرض توضيحي:** مكان لـ GIF/فيديو قصير (`assets/img/demo.gif`).

**7. شريط تحميل ختامي:** عنوان «جرّبه الآن» + زر تحميل + متطلبات النظام.

**8. الفوتر:** روابط (الخصوصية · GRW Lab · تواصل · السوشيال) + **التذييل القانوني**:
«Claude RTL أداة مستقلة من GRW Lab، غير تابعة لشركة Anthropic ولا مُعتمَدة منها. ‹Claude› علامة تجارية مملوكة لشركة Anthropic.»

**زر التحميل:** يشير إلى `/download/ClaudeRTL-1.0.0.dmg`. (`app.js` يكشف macOS ويظهر الإصدار.)

---

## 4) صفحة الخصوصية `privacy.html` (فصحى)
- العنوان: **سياسة الخصوصية**
- النص: «Claude RTL يعمل بالكامل على جهازك. يقرأ النص الذي تحدّده أو تنسخه فقط لعرضه بالاتجاه الصحيح داخل فقاعة العرض. لا يُرسِل التطبيق أي نصوص أو بيانات إلى أي خادم، ولا يجمع أي معلومات شخصية، ولا يستخدم أي تتبّع أو تحليلات. يُستخدَم إذن Accessibility حصريًا لاكتشاف تحديد النص محليًا. للتواصل أو الإبلاغ عن مشكلة: <support-email>.»
- «آخر تحديث: <التاريخ>».
- نفس الهوية (header/footer مشترك).

---

## 5) أساسيات مهمة
- **favicon** + apple-touch-icon من أيقونة التطبيق.
- **روابط تواصل/دعم**: إيميل دعم في الفوتر وصفحة الخصوصية.
- **صفحة 404** بسيطة بنفس الهوية.
- (اختياري لاحقًا) صفحة **سجل التغييرات/الإصدارات** و**شروط استخدام** مختصرة.
- SSL مفعّل على الصب دومين (Let's Encrypt من CWP).

---

## 6) الأصول المطلوبة (تتحط في `assets/img/` و`download/`)
- `logo.svg` / `icon.png` (أيقونة التطبيق).
- لقطات للبالون (عندنا من الجلسة).
- `demo.gif` (تسجيل شاشة قصير — يتعمل لاحقًا).
- `og-image.png` 1200×630 (للمشاركة).
- `ClaudeRTL-1.0.0.dmg` (الملف الموثّق من الـ runbook).

---

## 7) خطوات الاختبار + DoD
- [ ] الموقع RTL سليم بالكامل، والفصحى صحيحة، بدون أي نص إنجليزي مكسور.
- [ ] الهوية مطابقة للتطبيق (نفس الكوّرال/الكريمي/الخطوط)، فاتح وداكن يشتغلوا تلقائيًا.
- [ ] متجاوب على موبايل/تابلت/ديسكتوب.
- [ ] زر التحميل بيجيب الـ DMG فعلًا.
- [ ] صفحة الخصوصية والفوتر فيهم الـ disclaimer.
- [ ] أنيميشن الكشف عند التمرير شغّال (CSS + IntersectionObserver، بدون مكتبات).
- [ ] بطاقات OG/meta تظهر صح عند مشاركة الرابط.
- [ ] Lighthouse: أداء/وصول جيد.
**DoD:** موقع كامل قابل للرفع مباشرةً على docroot الصب دومين، وزر التحميل شغّال، والهوية موحّدة مع التطبيق.
