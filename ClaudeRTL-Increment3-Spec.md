# Claude RTL — Increment 3 (Behavior fixes + Claude rebrand + wow feature)

> سبيك لـ Cursor. كل بند: تنفيذ + اختبار + DoD. يبني فوق النسخة الحالية.

---

## 1) إلغاء الإغلاق التلقائي
**المشكلة:** البالون بيقفل لوحده وأنا بقرأ.
**التنفيذ:** شيل أي auto-hide timer خالص (الـ 12s ومنطق mouseleave اللي بيقفل بعد 3.5s).
البالون يفضل مفتوح لحد ما المستخدم:
- يدوس **×**، أو
- يدوس **بره** البالون (global mouse-down monitor)، أو
- يضغط **Esc**.
**اختبار:** افتح البالون وسيبه 60 ثانية وأنت بتقرأ → يفضل مفتوح. يقفل بس بالطرق التلاتة فوق.
**DoD:** مفيش إغلاق تلقائي بالوقت إطلاقًا.

---

## 2) إصلاح: زرار النسخ بيحرّك البالون بعيد
**السبب:** زرار النسخ بيكتب في الحافظة → مراقب الحافظة (changeCount poll) بيحس إنه نص جديد → بيعيد إظهار البالون عند مكان الماوس الحالي (فوق الزرار) → يقفز.
**التنفيذ:** بعد أي كتابة برمجية للحافظة من جوّه التطبيق (زرار النسخ، أو selection-capture)، **حدّث `lastChangeCount` فورًا** لقيمة `NSPasteboard.general.changeCount` الحالية، عشان البول ما يعتبرهاش تغيير جديد. (أو suppress window 1s زي اللي في البروتوتايب.) النسخ **ما يحرّكش** البالون نهائيًا.
**اختبار:** افتح البالون، دوس نسخ → النص في الحافظة، والبالون **مكانه ما يتغيّرش** أبدًا.
**DoD:** زرار النسخ بينسخ بس، من غير أي حركة للبالون.

---

## 3) Rebrand بهوية Claude (مع إبقائه أداة GRW مستقلة)
**التنفيذ (`bubble.html` + `onboarding.html`):** بدّل متغيّرات الألوان لباليتة Claude،
ودعم فاتح/داكن تلقائي حسب نظام الماك عبر `prefers-color-scheme`:

```css
:root{
  --accent:#C15F3C;          /* Claude coral (Crail) */
  --accent-soft:#D4A27F;     /* warm tan */
}
/* الوضع الفاتح (هوية Claude الأساسية) */
@media (prefers-color-scheme: light){
  :root{
    --bg:#FAF9F5; --card:#FFFFFF; --surface:#F4F3EE;  /* Pampas */
    --ink:#191919; --muted:#6B675F; --line:#E7E3D8;
    --code-bg:#F4F3EE;
  }
}
/* الوضع الداكن (دافي مطابق لإحساس Claude) */
@media (prefers-color-scheme: dark){
  :root{
    --bg:#262220; --card:#2E2A27; --surface:#211E1C;
    --ink:#F4F3EE; --muted:#B1ADA1; --line:#3A3531;
    --code-bg:#1B1815; --accent:#D97757;
  }
}
```
- طبّق المتغيّرات على البالون: الخلفية `--card`، النص `--ink`، الشريط الجانبي والهيدر `--accent`، الحدود `--line`، الكود `--code-bg`.
- الخط: للنص العادي ممكن نقرّبه من إحساس Claude (serif دافي) لكن **خلّي العربي بخط النظام الواضح** (`-apple-system,"SF Arabic"`) للقراءة؛ ممكن العناوين serif.
- **مهم (هوية + قانوني):** في الـ Onboarding/About، فوتر ثابت:
  "Claude RTL — أداة مستقلة من GRW Lab. غير تابعة لـ Anthropic ولا مدعومة منها.
   ‘Claude’ علامة تجارية لـ Anthropic."
  وما نستخدمش لوجو Claude الرسمي — نعمل أيقونتنا الخاصة بروح الباليتة.
**اختبار:** بدّل نظام الماك فاتح/داكن → البالون يتبدّل تلقائي بنفس إحساس Claude. الفوتر بالـ disclaimer ظاهر في الـ Onboarding.
**DoD:** باليتة Claude مطبّقة، فاتح/داكن تلقائي، disclaimer واضح، من غير أي لوجو رسمي لـ Claude.

---

## 4) ✦ ميزة "نبهر بيها الدنيا": قراءة بصوت مع تمييز الكلمة (Read-aloud karaoke)
أثناء النُطق، الكلمة اللي بتتقري **تتحدّد/تتلوّن** في البالون لحظة بلحظة — تجربة قراءة فريدة للعربي.
**التنفيذ:**
- في `Speech.swift` نفّذ `speechSynthesizer(_:willSpeakRangeOfSpeechString:utterance:)`.
- مع كل range، ابعت للـ webview: `window.highlightRange(location, length)`.
- في الـ JS: لفّ النص في عناصر قابلة للتحديد (لفّ النص الأصلي في spans على مستوى الكلمة قبل العرض، أو استخدم Range API)، وحدّد الكلمة الحالية بخلفية `--accent` خفيفة + سكّرول لطيف ليها.
- عند `didFinish`/`didCancel` أو الإيقاف: نادِ `window.clearHighlight()` و `window.setSpeaking(false)`.
- التمييز يشتغل مع الـ toggle (بند الصوت) — ▶ يبدأ القراءة والتمييز، ⏸ يوقفهم.
**اختبار:** دوس ▶ → الكلمات تتلوّن واحدة واحدة وهي بتتقري وبتعمل follow؛ ⏸ يوقف ويمسح التمييز.
**DoD:** تمييز الكلمة متزامن مع الصوت، بيتمسح عند الإيقاف، ومتداخلش مع الـ RTL/Markdown.

---

## ملاحظات للنشر (مش دلوقتي)
- Sandbox OFF + Hardened Runtime + Developer ID + notarize + DMG.
- الـ disclaimer يتكرر في صفحة الهبوط ووصف التحميل.
